<!DOCTYPE html>
<html lang="en" class="h-full">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- SEO Meta Tags -->
    <meta name="description"
        content="Password hashing library. SHA-256, SHA-384, SHA-512 support. Automatic salting and security upgrades.">
    <meta name="keywords" content="password hashing, sha-256, sha-384, sha-512, authentication, security">
    <meta name="robots" content="index, follow">
    <meta name="author" content="Inslash">
    <link rel="canonical" href="https://inslash.antqr.xyz">

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://inslash.antqr.xyz">
    <meta property="og:title" content="Inslash - Password Hashing Library">
    <meta property="og:description"
        content="Password hashing library with SHA-256/384/512 support and automatic security upgrades.">
    <meta property="og:image" content="https://inslash.antqr.xyz/images/128x128.png">

    <!-- Twitter -->
    <meta property="twitter:card" content="summary">
    <meta property="twitter:url" content="https://inslash.antqr.xyz">
    <meta property="twitter:title" content="Inslash - Password Hashing Library">
    <meta property="twitter:description"
        content="Password hashing library with SHA-256/384/512 support and automatic security upgrades.">
    <meta property="twitter:image" content="https://inslash.antqr.xyz/images/128x128.png">

    <title>
        <%= title %> | Inslash
    </title>
    <link href="/css/styles.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="icon" type="image/png" sizes="32x32" href="/images/128x128.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/images/128x128.png">
    <link rel="shortcut icon" href="/images/logo.png">

    <!-- JSON-LD Structured Data -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "SoftwareApplication",
      "name": "Inslash",
      "description": "Password hashing library with SHA-256, SHA-384, and SHA-512 support",
      "applicationCategory": "DeveloperApplication",
      "operatingSystem": "Node.js",
      "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "USD"
      }
    }
    </script>

    <!-- Theme initialization (must run before page renders to prevent flash) -->
    <script>
        (function () {
            const theme = localStorage.getItem('theme');
            const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;

            if (theme === 'dark' || (theme === 'system' && systemPrefersDark) || (!theme && systemPrefersDark)) {
                document.documentElement.classList.add('dark');
            }
        })();
    </script>
    <!-- Alpine.js for lightweight interactions -->
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <style>
        ::selection {
            background-color: rgba(99, 102, 241, 0.1);
            color: #4f46e5;
        }

        ::-moz-selection {
            background-color: rgba(99, 102, 241, 0.1);
            color: #4f46e5;
        }
    </style>
</head>

<body
    class="bg-gray-50 dark:bg-gray-900 <%= locals.fullWidth ? '' : 'bg-grid' %> min-h-full flex flex-col font-sans text-gray-900 dark:text-gray-100 antialiased relative transition-colors duration-200">

    <!-- Background Effects -->
    <% if (!locals.fullWidth) { %>
        <div class="fixed inset-0 overflow-hidden pointer-events-none -z-10">
            <!-- Subtle Mesh Gradient -->
            <div
                class="absolute top-0 left-0 w-full h-full opacity-30 dark:opacity-20 bg-[radial-gradient(circle_at_50%_0%,rgba(99,102,241,0.08)_0%,transparent_50%)]">
            </div>
            <!-- Precision Grid Pattern -->
            <div
                class="absolute inset-0 bg-[linear-gradient(to_right,#80808008_1px,transparent_1px),linear-gradient(to_bottom,#80808008_1px,transparent_1px)] bg-[size:32px_32px]">
            </div>
        </div>
        <% } %>


            <!-- Navigation -->
            <nav class="sticky top-0 z-[100] bg-white/50 dark:bg-[#020617]/50 backdrop-blur-2xl border-b border-slate-200/60 dark:border-white/5"
                x-data="{ mobileMenuOpen: false, userDropdownOpen: false }">
                <div class="<%= locals.fullWidth ? 'w-full px-6' : 'max-w-7xl mx-auto px-6' %>">
                    <div class="flex justify-between h-16">
                        <!-- Logo & Desktop Nav -->
                        <div class="flex items-center gap-12">
                            <a href="/" class="flex items-center gap-3 group">
                                <img src="/images/128x128.png" alt="Inslash Icon"
                                    class="w-8 h-8 rounded-lg shadow-2xl border border-white/10 group-hover:scale-105 transition-transform duration-500">

                            </a>

                            <div class="hidden md:flex items-center gap-2">
                                <% if (locals.currentUser) { %>
                                    <a href="/dashboard" class="nav-link-saas px-4 py-2">Dashboard</a>
                                    <a href="/projects" class="nav-link-saas px-4 py-2">Projects</a>
                                    <a href="/api-keys" class="nav-link-saas px-4 py-2">API Keys</a>
                                    <a href="/console" class="nav-link-saas px-4 py-2">Sandbox</a>
                                    <% } else { %>
                                        <a href="/docs" class="nav-link-saas px-4 py-2">Documentation</a>
                                        <a href="/docs#security" class="nav-link-saas px-4 py-2">Security</a>
                                        <a href="/docs#api" class="nav-link-saas px-4 py-2">API Specification</a>
                                        <% } %>
                            </div>
                        </div>

                        <!-- Right Side Actions -->
                        <div class="flex items-center gap-6">
                            <% if (locals.currentUser) { %>
                                <div class="relative" @click.away="userDropdownOpen = false">
                                    <button @click="userDropdownOpen = !userDropdownOpen"
                                        class="flex items-center gap-2.5 px-2.5 py-1.5 bg-slate-50 dark:bg-white/5 border border-slate-200 dark:border-white/10 rounded-full hover:bg-slate-100 dark:hover:bg-white/10 transition-all duration-300 group">
                                        <div
                                            class="w-7 h-7 bg-slate-900 rounded-full flex items-center justify-center text-white dark:bg-white dark:text-slate-900 text-[10px] font-black uppercase shadow-lg">
                                            <%= currentUser.username.charAt(0) %>
                                        </div>
                                        <svg class="w-4 h-4 text-slate-400 group-hover:text-slate-600 dark:group-hover:text-slate-200 transition-colors"
                                            fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5"
                                                d="M19 9l-7 7-7-7"></path>
                                        </svg>
                                    </button>

                                    <!-- Dropdown Menu -->
                                    <div x-show="userDropdownOpen" x-transition:enter="transition ease-out duration-200"
                                        x-transition:enter-start="opacity-0 translate-y-1"
                                        x-transition:enter-end="opacity-100 translate-y-0"
                                        class="absolute right-0 mt-3 w-64 bg-white dark:bg-[#0a0a0a] border border-slate-200 dark:border-white/10 rounded-2xl shadow-2xl py-2 z-50 overflow-hidden"
                                        style="display: none;">

                                        <!-- User Info -->
                                        <div
                                            class="px-5 py-4 border-b border-slate-100 dark:border-white/5 mb-1 bg-slate-50/50 dark:bg-white/[0.02]">
                                            <p
                                                class="text-[10px] font-bold uppercase tracking-widest text-slate-400 mb-1">
                                                Authenticated As</p>
                                            <p class="text-xs font-bold text-slate-900 dark:text-white truncate">
                                                <%= currentUser.username %>
                                            </p>
                                            <p class="text-[11px] text-slate-500 dark:text-slate-400 truncate mt-0.5">
                                                <%= currentUser.email %>
                                            </p>
                                        </div>

                                        <!-- Menu Links -->
                                        <a href="/profile"
                                            class="flex items-center gap-3 px-5 py-2.5 text-sm text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-white hover:bg-slate-50 dark:hover:bg-white/5 transition-colors">
                                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                                            </svg>
                                            Profile Settings
                                        </a>
                                        <a href="/api-keys"
                                            class="flex items-center gap-3 px-5 py-2.5 text-sm text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-white hover:bg-slate-50 dark:hover:bg-white/5 transition-colors">
                                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1.01 0 01.293-.707l5.964-5.964A6 6 0 1121 9z" />
                                            </svg>
                                            Security & API
                                        </a>

                                        <div class="h-px bg-slate-100 dark:bg-white/5 my-1 mx-5"></div>

                                        <button onclick="toggleTheme()"
                                            class="w-full flex items-center justify-between px-5 py-2.5 text-sm text-slate-600 dark:text-slate-400 hover:text-slate-900 dark:hover:text-white hover:bg-slate-50 dark:hover:bg-white/5 transition-colors">
                                            <div class="flex items-center gap-3">
                                                <svg class="w-4 h-4 dark:hidden" fill="none" stroke="currentColor"
                                                    viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round"
                                                        stroke-width="2"
                                                        d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364-6.364l-.707.707M6.343 17.657l-.707.707m12.728 0l-.707-.707M6.343 6.343l-.707-.707M12 5a7 7 0 100 14 7 7 0 000-14z" />
                                                </svg>
                                                <svg class="w-4 h-4 hidden dark:block" fill="none" stroke="currentColor"
                                                    viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round"
                                                        stroke-width="2"
                                                        d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" />
                                                </svg>
                                                Appearance
                                            </div>
                                            <span
                                                class="text-[10px] font-bold uppercase tracking-wider bg-slate-100 dark:bg-white/10 px-1.5 py-0.5 rounded">Toggle</span>
                                        </button>

                                        <div class="h-px bg-slate-100 dark:bg-white/5 my-1 mx-5"></div>

                                        <a href="/logout"
                                            class="flex items-center gap-3 px-5 py-3 text-sm text-red-500 hover:bg-red-50 dark:hover:bg-red-500/10 transition-colors font-medium">
                                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                                    d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                                            </svg>
                                            Sign Out
                                        </a>
                                    </div>
                                </div>
                                <% } else { %>
                                    <div class="hidden md:flex items-center gap-8">
                                        <a href="/login"
                                            class="text-[10px] font-black uppercase tracking-[0.3em] text-slate-400 hover:text-slate-900 dark:hover:text-white transition-colors">Sign
                                            In</a>
                                        <a href="/signup" class="btn-primary-mini px-6 py-2.5">Get Started</a>
                                    </div>
                                    <% } %>

                                        <!-- Mobile Toggle -->
                                        <button @click="mobileMenuOpen = !mobileMenuOpen"
                                            class="md:hidden p-2 text-slate-400 hover:text-white transition-colors">
                                            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5"
                                                    d="M4 6h16M4 12h16M4 18h16"></path>
                                            </svg>
                                        </button>
                        </div>
                    </div>
                </div>

                <!-- Mobile Menu -->
                <div x-show="mobileMenuOpen"
                    class="md:hidden border-t border-slate-200 dark:border-white/5 bg-white dark:bg-[#0a0a0a] px-6 py-6 space-y-4"
                    style="display: none;">
                    <% if (locals.currentUser) { %>
                        <a href="/dashboard"
                            class="block text-sm font-bold text-slate-600 dark:text-slate-400">Dashboard</a>
                        <a href="/projects"
                            class="block text-sm font-bold text-slate-600 dark:text-slate-400">Projects</a>
                        <a href="/logout" class="block text-sm font-black uppercase tracking-widest text-red-500">Sign
                            Out</a>
                        <% } else { %>
                            <a href="/login" class="block text-sm font-bold text-slate-600 dark:text-slate-400">Sign
                                In</a>
                            <a href="/signup" class="btn-primary w-full text-center">Get Started</a>
                            <% } %>
                </div>
            </nav>

            <!-- Main Content -->
            <main class="<%= locals.fullWidth ? '' : 'flex-grow max-w-7xl w-full mx-auto px-6 py-12' %>">
                <% if (!locals.fullWidth) { %>
                    <!-- Flash Messages -->
                    <% if (locals.error && error.length) { %>
                        <div class="alert animate-slideInDown mb-8 bg-red-50 dark:bg-red-500/10 border border-red-200 dark:border-red-500/20 p-4 rounded-2xl flex items-center justify-between"
                            x-data="{ show: true }" x-show="show">
                            <div class="flex items-center gap-3">
                                <svg class="h-5 w-5 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5"
                                        d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z">
                                    </path>
                                </svg>
                                <span class="text-sm font-bold text-red-800 dark:text-red-400 tracking-tight">
                                    <%= error %>
                                </span>
                            </div>
                            <button @click="show = false" class="text-red-400 hover:text-red-600"><svg class="w-4 h-4"
                                    fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3"
                                        d="M6 18L18 6M6 6l12 12"></path>
                                </svg></button>
                        </div>
                        <% } %>

                            <% if (locals.success && success.length) { %>
                                <div class="alert animate-slideInDown mb-8 bg-emerald-50 dark:bg-emerald-500/10 border border-emerald-200 dark:border-emerald-500/20 p-4 rounded-2xl flex items-center justify-between"
                                    x-data="{ show: true }" x-show="show" x-init="setTimeout(() => show = false, 5000)">
                                    <div class="flex items-center gap-3">
                                        <svg class="h-5 w-5 text-emerald-500" fill="none" stroke="currentColor"
                                            viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5"
                                                d="M5 13l4 4L19 7"></path>
                                        </svg>
                                        <span
                                            class="text-sm font-bold text-emerald-800 dark:text-emerald-400 tracking-tight">
                                            <%= success %>
                                        </span>
                                    </div>
                                    <button @click="show = false" class="text-emerald-400 hover:text-emerald-600"><svg
                                            class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3"
                                                d="M6 18L18 6M6 6l12 12"></path>
                                        </svg></button>
                                </div>
                                <% } %>
                                    <% } %>

                                        <%- body %>
            </main>

            <footer class="bg-white dark:bg-[#050505] border-t border-slate-200 dark:border-white/5 py-20">
                <div class="max-w-7xl mx-auto px-6">
                    <div class="grid grid-cols-1 md:grid-cols-4 gap-12 text-sm">
                        <div class="col-span-1 md:col-span-1 space-y-6">
                            <a href="/" class="flex items-center gap-3 group">
                                <img src="/images/128x128.png" alt="Inslash Icon"
                                    class="w-7 h-7 rounded-lg shadow-xl opacity-80 group-hover:opacity-100 transition-opacity">

                            </a>
                            <p class="text-slate-500 dark:text-slate-400 font-medium leading-relaxed">
                                The world's fastest security sequence layer.
                                Designed for high-fidelity authentication and zero-trust infrastructure.
                            </p>
                        </div>

                        <div>
                            <h4 class="text-[10px] font-black uppercase tracking-widest text-slate-400 mb-6">Resources
                            </h4>
                            <ul class="space-y-4">
                                <li><a href="/docs" class="footer-link">Documentation</a></li>
                                <li><a href="/docs#api" class="footer-link">API Specification</a></li>
                                <li><a href="/console" class="footer-link">Terminal Console</a></li>
                            </ul>
                        </div>

                        <div>
                            <h4 class="text-[10px] font-black uppercase tracking-widest text-slate-400 mb-6">
                                Infrastructure</h4>
                            <ul class="space-y-4">
                                <li><a href="https://github.com/reshuk-code/inslash" class="footer-link">Open Source</a>
                                </li>
                                <li><a href="https://www.npmjs.com/package/inslash" class="footer-link">NPM Registry</a>
                                </li>
                                <li><a href="/docs#cli" class="footer-link">CLI Tools</a></li>
                            </ul>
                        </div>

                        <div>
                            <h4 class="text-[10px] font-black uppercase tracking-widest text-slate-400 mb-6">Legal</h4>
                            <ul class="space-y-4">
                                <li><a href="/privacy" class="footer-link">Privacy Policy</a></li>
                                <li><a href="/terms" class="footer-link">Terms of Service</a></li>
                                <li><a href="/docs#compliance" class="footer-link">Compliance</a></li>
                            </ul>
                        </div>
                    </div>

                    <div
                        class="mt-20 pt-8 border-t border-slate-100 dark:border-white/5 flex flex-col md:flex-row justify-between gap-4">
                        <p class="text-[10px] font-black uppercase tracking-widest text-slate-400">
                            &copy; <%= new Date().getFullYear() %> Inslash Security Protocol. Powered by the antqr
                                network.
                        </p>
                        <div class="flex gap-6">
                            <span
                                class="flex items-center gap-1.5 text-[10px] font-black uppercase tracking-widest text-emerald-500">
                                <span class="w-1.5 h-1.5 rounded-full bg-emerald-500 animate-pulse"></span>
                                All Systems Nominal
                            </span>
                        </div>
                    </div>
                </div>
            </footer>

            <!-- Global Modal Component -->
            <div id="globalModal" class="fixed inset-0 z-50 hidden overflow-y-auto" aria-labelledby="modal-title"
                role="dialog" aria-modal="true">
                <div class="flex items-center justify-center min-h-screen pt-4 px-4 pb-20 text-center sm:block sm:p-0">
                    <!-- Background overlay -->
                    <div class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity animate-fadeIn"
                        onclick="closeModal()"></div>

                    <!-- Modal panel -->
                    <div
                        class="inline-block align-bottom bg-white dark:bg-gray-800 rounded-2xl text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full animate-scaleIn">
                        <div class="bg-white dark:bg-gray-800 px-4 pt-5 pb-4 sm:p-6 sm:pb-4">
                            <div class="sm:flex sm:items-start">
                                <div id="modalIcon"
                                    class="mx-auto flex-shrink-0 flex items-center justify-center h-12 w-12 rounded-full bg-red-100 sm:mx-0 sm:h-10 sm:w-10">
                                    <!-- Icon will be inserted here -->
                                </div>
                                <div class="mt-3 text-center sm:mt-0 sm:ml-4 sm:text-left flex-1">
                                    <h3 class="text-lg leading-6 font-semibold text-gray-900 dark:text-gray-100"
                                        id="modalTitle">
                                        Modal Title
                                    </h3>
                                    <div class="mt-2">
                                        <p class="text-sm text-gray-500 dark:text-gray-400" id="modalMessage">
                                            Modal message
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="bg-gray-50 dark:bg-gray-700 px-4 py-3 sm:px-6 sm:flex sm:flex-row-reverse gap-2">
                            <button type="button" id="modalConfirm" class="btn-primary w-full sm:w-auto">
                                Confirm
                            </button>
                            <button type="button" onclick="closeModal()"
                                class="btn-secondary w-full sm:w-auto mt-3 sm:mt-0">
                                Cancel
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <script>
                // Global modal functions
                function showModal({ title, message, type = 'danger', confirmText = 'Confirm', onConfirm }) {
                    const modal = document.getElementById('globalModal');
                    const modalTitle = document.getElementById('modalTitle');
                    const modalMessage = document.getElementById('modalMessage');
                    const modalIcon = document.getElementById('modalIcon');
                    const modalConfirm = document.getElementById('modalConfirm');

                    // Set content
                    modalTitle.textContent = title;
                    modalMessage.textContent = message;
                    modalConfirm.textContent = confirmText;

                    // Set icon and colors based on type
                    const icons = {
                        danger: {
                            bg: 'bg-red-100 dark:bg-red-900/30',
                            svg: '<svg class="h-6 w-6 text-red-600 dark:text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path></svg>'
                        },
                        success: {
                            bg: 'bg-green-100 dark:bg-green-900/30',
                            svg: '<svg class="h-6 w-6 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>'
                        },
                        info: {
                            bg: 'bg-blue-100 dark:bg-blue-900/30',
                            svg: '<svg class="h-6 w-6 text-blue-600 dark:text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>'
                        }
                    };

                    const iconConfig = icons[type] || icons.danger;
                    modalIcon.className = `mx-auto flex-shrink-0 flex items-center justify-center h-12 w-12 rounded-full ${iconConfig.bg} sm:mx-0 sm:h-10 sm:w-10`;
                    modalIcon.innerHTML = iconConfig.svg;

                    // Set confirm button style
                    if (type === 'danger') {
                        modalConfirm.className = 'btn-danger w-full sm:w-auto';
                    } else {
                        modalConfirm.className = 'btn-primary w-full sm:w-auto';
                    }

                    // Set confirm handler
                    modalConfirm.onclick = () => {
                        closeModal();
                        if (onConfirm) onConfirm();
                    };

                    // Show modal
                    modal.classList.remove('hidden');
                    document.body.style.overflow = 'hidden';
                }

                function closeModal() {
                    const modal = document.getElementById('globalModal');
                    modal.classList.add('hidden');
                    document.body.style.overflow = '';
                }

                // Show toast notification (for non-blocking messages)
                function showToast(message, type = 'success') {
                    const toast = document.createElement('div');
                    const colors = {
                        success: 'bg-green-50 border-green-200 text-green-800 dark:bg-green-900/30 dark:border-green-800 dark:text-green-300',
                        error: 'bg-red-50 border-red-200 text-red-800 dark:bg-red-900/30 dark:border-red-800 dark:text-red-300',
                        info: 'bg-blue-50 border-blue-200 text-blue-800 dark:bg-blue-900/30 dark:border-blue-800 dark:text-blue-300'
                    };

                    toast.className = `fixed top-4 right-4 z-50 px-4 py-3 rounded-lg border shadow-lg ${colors[type] || colors.info} animate-slideInDown backface-hidden`;
                    toast.textContent = message;
                    document.body.appendChild(toast);

                    setTimeout(() => {
                        toast.classList.add('opacity-0', 'transition-opacity');
                        setTimeout(() => toast.remove(), 300);
                    }, 3000);
                }

                // Theme toggle function
                function toggleTheme() {
                    const html = document.documentElement;
                    const isDark = html.classList.contains('dark');
                    const themeIcon = document.getElementById('themeIcon');

                    if (isDark) {
                        html.classList.remove('dark');
                        localStorage.setItem('theme', 'light');
                        // Sun icon
                        themeIcon.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path>';
                    } else {
                        html.classList.add('dark');
                        localStorage.setItem('theme', 'dark');
                        // Moon icon
                        themeIcon.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"></path>';
                    }
                }

                // Set initial icon based on current theme
                window.addEventListener('DOMContentLoaded', () => {
                    const themeIcon = document.getElementById('themeIcon');
                    if (themeIcon && document.documentElement.classList.contains('dark')) {
                        themeIcon.innerHTML = '<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"></path>';
                    }
                });
            </script>
</body>

</html>